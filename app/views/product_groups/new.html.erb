<% provide(:title, 'Neue Produkt Gruppe anlegen') %>
<h1>Neue Produkt Gruppe anlegen</h1>

<div class="row">
  <div class="span6 offset3">
    <%#<%= form_for(@product_group) do |f| %>%>
      <%#<%= render 'fields', f: f %>%>
      <%#<%= f.submit "Produktgruppe anlegen", class: "btn btn-large btn-primary" %>%>
    <%#<% end %>%>
  </div>
</div>


